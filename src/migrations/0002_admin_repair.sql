-- SHAZO RIDE ADMIN/API REPAIR MIGRATION
-- Safe to run on partially-created databases. Adds missing columns without dropping data.

ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS full_name VARCHAR(255);
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS email VARCHAR(255);
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS phone VARCHAR(50);
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS avatar_url VARCHAR(512);
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS role VARCHAR(50) DEFAULT 'customer';
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS is_verified BOOLEAN DEFAULT false;
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS password_hash VARCHAR(255);
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE IF EXISTS users ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS sessions (
  id VARCHAR(80) PRIMARY KEY,
  user_id VARCHAR(80) NOT NULL,
  role VARCHAR(50) NOT NULL,
  token TEXT UNIQUE NOT NULL,
  expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE IF EXISTS sessions ADD COLUMN IF NOT EXISTS user_id VARCHAR(80);
ALTER TABLE IF EXISTS sessions ADD COLUMN IF NOT EXISTS role VARCHAR(50);
ALTER TABLE IF EXISTS sessions ADD COLUMN IF NOT EXISTS token TEXT;
ALTER TABLE IF EXISTS sessions ADD COLUMN IF NOT EXISTS expires_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE IF EXISTS sessions ADD COLUMN IF NOT EXISTS created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS service_settings (
  id VARCHAR(80) PRIMARY KEY,
  service_type VARCHAR(50) UNIQUE NOT NULL,
  base_fare NUMERIC(10,2) NOT NULL DEFAULT 0,
  per_km_rate NUMERIC(10,2) NOT NULL DEFAULT 0,
  per_minute_rate NUMERIC(10,2) NOT NULL DEFAULT 0,
  minimum_fare NUMERIC(10,2) NOT NULL DEFAULT 0,
  commission_percentage NUMERIC(5,2) NOT NULL DEFAULT 0,
  commission_fixed NUMERIC(10,2) NOT NULL DEFAULT 0,
  is_active BOOLEAN NOT NULL DEFAULT true,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS service_type VARCHAR(50);
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS base_fare NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS per_km_rate NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS per_minute_rate NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS minimum_fare NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS commission_percentage NUMERIC(5,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS commission_fixed NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT true;
ALTER TABLE IF EXISTS service_settings ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS customer_profiles (
  user_id VARCHAR(80) PRIMARY KEY,
  rating NUMERIC(3,2) NOT NULL DEFAULT 5.00,
  completed_rides_count INTEGER NOT NULL DEFAULT 0,
  emergency_contact_phone VARCHAR(50),
  emergency_contact_name VARCHAR(100),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS rider_profiles (
  user_id VARCHAR(80) PRIMARY KEY,
  verification_status VARCHAR(50) NOT NULL DEFAULT 'pending',
  license_number VARCHAR(100),
  cnic_number VARCHAR(100),
  vehicle_type VARCHAR(50),
  is_online BOOLEAN NOT NULL DEFAULT false,
  latitude NUMERIC(10,7),
  longitude NUMERIC(10,7),
  last_location_update TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS verification_status VARCHAR(50) DEFAULT 'pending';
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS vehicle_type VARCHAR(50);
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS is_online BOOLEAN DEFAULT false;
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS latitude NUMERIC(10,7);
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS longitude NUMERIC(10,7);
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS last_location_update TIMESTAMP WITH TIME ZONE;
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE IF EXISTS rider_profiles ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS rider_wallets (
  rider_id VARCHAR(80) PRIMARY KEY,
  balance NUMERIC(10,2) NOT NULL DEFAULT 0.00,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS rider_wallet_ledger (
  id VARCHAR(80) PRIMARY KEY,
  rider_id VARCHAR(80),
  amount NUMERIC(10,2) NOT NULL DEFAULT 0,
  transaction_type VARCHAR(50) NOT NULL DEFAULT 'manual_adjustment',
  reference_id VARCHAR(80),
  note TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS restaurant_profiles (
  id VARCHAR(80) PRIMARY KEY,
  owner_id VARCHAR(80),
  name VARCHAR(255) NOT NULL DEFAULT 'Restaurant',
  cuisine_type VARCHAR(255) NOT NULL DEFAULT 'General',
  address VARCHAR(512) NOT NULL DEFAULT '',
  latitude NUMERIC(10,7) DEFAULT 24.8607,
  longitude NUMERIC(10,7) DEFAULT 67.0011,
  service_radius_meters NUMERIC(10,2) NOT NULL DEFAULT 5000.00,
  opening_hours VARCHAR(100) NOT NULL DEFAULT '09:00 - 23:00',
  is_active BOOLEAN NOT NULL DEFAULT true,
  image_url VARCHAR(512),
  rating NUMERIC(3,2) NOT NULL DEFAULT 5.00,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS owner_id VARCHAR(80);
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS name VARCHAR(255) DEFAULT 'Restaurant';
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS cuisine_type VARCHAR(255) DEFAULT 'General';
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS address VARCHAR(512) DEFAULT '';
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS latitude NUMERIC(10,7) DEFAULT 24.8607;
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS longitude NUMERIC(10,7) DEFAULT 67.0011;
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT true;
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS rating NUMERIC(3,2) DEFAULT 5.00;
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE IF EXISTS restaurant_profiles ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS ride_bookings (
  id VARCHAR(80) PRIMARY KEY,
  customer_id VARCHAR(80),
  rider_id VARCHAR(80),
  pickup_address VARCHAR(512) DEFAULT '',
  pickup_lat NUMERIC(10,7),
  pickup_lng NUMERIC(10,7),
  dropoff_address VARCHAR(512) DEFAULT '',
  dropoff_lat NUMERIC(10,7),
  dropoff_lng NUMERIC(10,7),
  distance_km NUMERIC(6,2) DEFAULT 0,
  duration_minutes INTEGER DEFAULT 0,
  ride_type VARCHAR(50) DEFAULT 'bike',
  fare NUMERIC(10,2) DEFAULT 0,
  commission_amount NUMERIC(10,2) DEFAULT 0,
  payment_method VARCHAR(50) DEFAULT 'cash',
  status VARCHAR(50) DEFAULT 'requested',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS customer_id VARCHAR(80);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS rider_id VARCHAR(80);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS pickup_address VARCHAR(512) DEFAULT '';
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS pickup_lat NUMERIC(10,7);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS pickup_lng NUMERIC(10,7);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS dropoff_address VARCHAR(512) DEFAULT '';
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS dropoff_lat NUMERIC(10,7);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS dropoff_lng NUMERIC(10,7);
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS distance_km NUMERIC(6,2) DEFAULT 0;
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS duration_minutes INTEGER DEFAULT 0;
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS ride_type VARCHAR(50) DEFAULT 'bike';
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS fare NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS commission_amount NUMERIC(10,2) DEFAULT 0;
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS payment_method VARCHAR(50) DEFAULT 'cash';
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS status VARCHAR(50) DEFAULT 'requested';
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE IF EXISTS ride_bookings ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP;

CREATE TABLE IF NOT EXISTS ambulance_bookings (
  id VARCHAR(80) PRIMARY KEY,
  customer_id VARCHAR(80),
  patient_name VARCHAR(255) DEFAULT '',
  contact_number VARCHAR(100) DEFAULT '',
  pickup_address VARCHAR(512) DEFAULT '',
  pickup_lat NUMERIC(10,7),
  pickup_lng NUMERIC(10,7),
  destination_hospital VARCHAR(255) DEFAULT '',
  emergency_type VARCHAR(100) DEFAULT '',
  notes TEXT,
  fare_estimate NUMERIC(10,2) DEFAULT 0,
  assigned_driver_name VARCHAR(255),
  assigned_vehicle_plate VARCHAR(50),
  status VARCHAR(50) DEFAULT 'requested',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS food_orders (
  id VARCHAR(80) PRIMARY KEY,
  customer_id VARCHAR(80),
  restaurant_id VARCHAR(80),
  delivery_address VARCHAR(512) DEFAULT '',
  delivery_lat NUMERIC(10,7),
  delivery_lng NUMERIC(10,7),
  delivery_instructions TEXT,
  items_total NUMERIC(10,2) DEFAULT 0,
  delivery_fee NUMERIC(10,2) DEFAULT 0,
  commission_amount NUMERIC(10,2) DEFAULT 0,
  grand_total NUMERIC(10,2) DEFAULT 0,
  payment_method VARCHAR(50) DEFAULT 'cash',
  status VARCHAR(50) DEFAULT 'ordered',
  rider_id VARCHAR(80),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS manual_payment_accounts (
  id VARCHAR(80) PRIMARY KEY,
  bank_name VARCHAR(150) NOT NULL DEFAULT 'Manual Account',
  account_title VARCHAR(150) NOT NULL DEFAULT 'Shazo Ride',
  account_number VARCHAR(100) NOT NULL DEFAULT '',
  instructions TEXT,
  min_topup VARCHAR(50) NOT NULL DEFAULT '200',
  max_topup VARCHAR(50) NOT NULL DEFAULT '50000',
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS manual_topup_requests (
  id VARCHAR(80) PRIMARY KEY,
  rider_id VARCHAR(80),
  payment_account_id VARCHAR(80),
  amount NUMERIC(10,2) NOT NULL DEFAULT 0,
  method VARCHAR(100) NOT NULL DEFAULT 'manual',
  sender_name VARCHAR(150) NOT NULL DEFAULT '',
  sender_phone VARCHAR(50) NOT NULL DEFAULT '',
  transaction_id VARCHAR(100),
  screenshot_url TEXT,
  note TEXT,
  status VARCHAR(50) NOT NULL DEFAULT 'pending',
  rejection_reason TEXT,
  reviewed_by VARCHAR(80),
  reviewed_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS free_ride_campaigns (
  id VARCHAR(80) PRIMARY KEY,
  name VARCHAR(150) NOT NULL DEFAULT 'Campaign',
  service_type VARCHAR(50) NOT NULL DEFAULT 'bike',
  quota_total INTEGER NOT NULL DEFAULT 0,
  quota_used INTEGER NOT NULL DEFAULT 0,
  allowed_zones TEXT DEFAULT 'all',
  start_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  end_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT (CURRENT_TIMESTAMP + INTERVAL '30 days'),
  status VARCHAR(50) NOT NULL DEFAULT 'active',
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS city_zones (
  id VARCHAR(80) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  city VARCHAR(100) NOT NULL DEFAULT 'Karachi',
  polygon_coordinates TEXT,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS support_tickets (
  id VARCHAR(80) PRIMARY KEY,
  user_id VARCHAR(80),
  source_type VARCHAR(50) DEFAULT 'customer',
  category VARCHAR(100) DEFAULT 'general',
  subject VARCHAR(255) NOT NULL DEFAULT 'Support Ticket',
  description TEXT DEFAULT '',
  priority VARCHAR(50) NOT NULL DEFAULT 'medium',
  status VARCHAR(50) NOT NULL DEFAULT 'open',
  assigned_to VARCHAR(80),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS ticket_messages (
  id VARCHAR(80) PRIMARY KEY,
  ticket_id VARCHAR(80),
  sender_id VARCHAR(80),
  message TEXT NOT NULL DEFAULT '',
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS safety_reports (
  id VARCHAR(80) PRIMARY KEY,
  booking_id VARCHAR(80),
  reported_by_id VARCHAR(80),
  reported_user_id VARCHAR(80),
  description TEXT NOT NULL DEFAULT '',
  is_emergency BOOLEAN NOT NULL DEFAULT false,
  investigation_status VARCHAR(50) NOT NULL DEFAULT 'pending',
  admin_notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS notifications (
  id VARCHAR(80) PRIMARY KEY,
  user_id VARCHAR(80),
  title VARCHAR(255) NOT NULL DEFAULT 'Notification',
  body TEXT NOT NULL DEFAULT '',
  is_read BOOLEAN NOT NULL DEFAULT false,
  data_payload TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS app_settings (
  key VARCHAR(100) PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO service_settings (id, service_type, base_fare, per_km_rate, per_minute_rate, minimum_fare, commission_percentage, commission_fixed)
VALUES
  ('set_bike', 'bike', 80, 35, 0, 120, 0, 0),
  ('set_car', 'car', 180, 70, 0, 300, 0, 0),
  ('set_ambulance', 'ambulance', 1500, 120, 0, 1500, 0, 0),
  ('set_food', 'food_delivery', 120, 25, 0, 120, 0, 0)
ON CONFLICT (service_type) DO NOTHING;

INSERT INTO city_zones (id, name, city, polygon_coordinates, is_active)
VALUES
  ('zone_dha', 'DHA Karachi', 'Karachi', NULL, true),
  ('zone_clifton', 'Clifton', 'Karachi', NULL, true),
  ('zone_gulshan', 'Gulshan-e-Iqbal', 'Karachi', NULL, true)
ON CONFLICT (id) DO NOTHING;
